<html lang="en">
<head>
  <title>{{title}}</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>
<div class="layout">
<header>
  <nav style="display: flex; justify-content: space-between; align-items: center;">
    <a href="/">Spring Demo</a>
    <div class="nav-links">
      {{#user}}
        <span>Welcome, <strong>{{firstname}}</strong>!</span>
        <a href="/new-article" style="margin-left: var(--space-4);">New Article</a>
        <a href="/logout" style="margin-left: var(--space-4);">Logout</a>
      {{/user}}
      {{^user}}
        <a href="/login" style="margin-right: var(--space-4);">Login</a>
        <a href="/register">Register</a>
      {{/user}}
    </div>
  </nav>
</header>
